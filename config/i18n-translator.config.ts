/**
 * Configuration for the i18n translator plugin
 */

const i18nTranslatorConfig = {
  // Configuration options for the i18n translator plugin
  // This would typically include settings for translation services, locales, etc.
  locales: ['ja-JP'], // Supported locales
  defaultLocale: 'en', // Default locale
  // Additional configuration as needed
};

export default i18nTranslatorConfig;