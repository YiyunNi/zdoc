---
title: "ANN検索の説明 | Cloud"
slug: /ann-search-explained
sidebar_label: "ANN検索の説明"
beta: FALSE
notebook: FALSE
description: "k近傍探索（kNN）は、クエリベクトルに最も近いk個のベクトルを見つけます。具体的には、k個の完全一致が見つかるまで、ベクトル空間内のすべてのベクトルとクエリベクトルを比較します。kNN検索は完璧な精度を保証しますが、特に高次元ベクトルで構成される大規模なデータセットでは、時間がかかります。 | Cloud"
type: origin
token: CWHGw3g9Ui9GEHkjhu2cHBOInXf
sidebar_position: 2
keywords: 
  - zilliz
  - ベクトルデータベース
  - cloud
  - ann search
  - milvus
  - ニューラルネットワーク
  - ディープラーニング
  - ナレッジベース
  - 自然言語処理

---

import Admonition from '@theme/Admonition';


# ANN検索の説明

k近傍探索（kNN）は、クエリベクトルに最も近いk個のベクトルを見つける検索です。具体的には、クエリベクトルをベクトル空間内のすべてのベクトルと比較し、k個の正確な一致が見つかるまで検索します。kNN検索は完璧な精度を保証しますが、特に高次元ベクトルを含む大規模なデータセットでは時間がかかります。

対照的に、近似近傍探索（ANN）は、事前にインデックスを構築する必要があります。さまざまなインデックスアルゴリズムは、検索速度、メモリ使用量、精度の間でトレードオフを示します。一般的に、これらのアルゴリズムを実装するには、検索範囲を絞り込む方法と、高次元ベクトル空間を低次元部分空間に分解する方法の2つのパスがあります。

検索範囲を絞り込むことで、クエリベクトルと比較する候補のサブセットのみを選択することで、検索時間を短縮できます。これにより、無関係なベクトルが除外されます。ベクトルがサブセットに含まれるかどうかを判断するには、ベクトルをソートするためのインデックス構造が必要です。

インデックス構造を形成するために利用できるアイデアは、一般的にグラフ、ツリー、ハッシュの3つです。

## HNSW：グラフベースのインデックスアルゴリズム{#hnsw-a-graph-based-indexing-algorithm}

階層的ナビゲーション可能スモールワールド（HNSW）は、階層的な近接グラフを作成することでベクトル空間をインデックス化します。具体的には、HNSWは各レイヤーでベクトル（または頂点）間に近接リンク（またはエッジ）を描画して単一レイヤーの近接グラフを形成し、それらを積み重ねて階層グラフを形成します。最下層にはすべてのベクトルとその近接リンクが含まれます。レイヤーが上がるにつれて、より小さなベクトルのセットと近接リンクのみが残ります。

階層的な近接グラフが作成されると、検索は次のように進行します。

1. 最上層でエントリポイントとしてベクトルを見つけます。

1. 利用可能な近接リンクに沿って、最も近いベクトルに徐々に移動します。

1. 最上層で最も近いベクトルを特定したら、そのベクトルをより低い層のエントリポイントとして使用し、その層で最も近い近傍を見つけます。

1. 最下層で最も近いベクトルが見つかるまで、前の手順を繰り返します。

![hnsw-explained](https://zdoc-images.s3.us-west-2.amazonaws.com/hnsw-explained.png "hnsw-explained")

## LSH：ハッシュベースのANNインデックスアルゴリズム{#lsh-a-hash-based-ann-indexing-algorithm}

局所性鋭敏型ハッシュ（LSH）は、さまざまなハッシュ関数を使用して任意の長さのデータピースを固定長の値としてハッシュにマッピングし、これらのハッシュをハッシュバケットに収集し、少なくとも一度は同じ値にハッシュされたベクトルを候補ペアとしてタグ付けすることで、ベクトル空間をインデックス化します。

![locality_sensitive_hashing](https://zdoc-images.s3.us-west-2.amazonaws.com/locality_sensitive_hashing.png "locality_sensitive_hashing")

## DiskANN：Vamanaグラフに基づくディスク上のANN検索{#diskann-ann-search-on-disk-based-on-vamana-graphs}

階層的な検索のために階層グラフを構築するHNSWとは異なり、Vamanaのインデックス作成プロセスは比較的単純です。

1. ランダムグラフを初期化します。

1. グローバルな重心を特定し、最も近い点を見つけることでナビゲーションポイントを見つけます。平均検索半径を最小化するためにグローバル比較を使用します。

1. 初期化されたランダム近傍グラフとステップ2からの検索開始点を使用して、近似近傍探索を実行します。検索パス上のすべての点を候補近傍セットとして使用し、検索半径を減らすためにアルファ=1のエッジトリミング戦略を適用します。

1. グラフの品質とリコール率を向上させるために、調整されたアルファ>1（論文では1.2を推奨）でステップ3を繰り返します。

インデックスが準備されると、検索は次のように進行します。

1. クエリセット、PQ中心点データ、コードブックデータ、検索開始点、インデックスメタを含む関連データをロードします。

1. インデックス付きデータセットを使用してcached_beam_searchを実行し、各点のアクセス回数をカウントし、アクセス頻度が最も高いnum_nodes_to_cache点をキャッシュします。

1. WARMUP操作は、デフォルトでサンプルデータセットを使用してcached_beam_searchを実行することで実行されます。

1. 各指定されたパラメータLに対してクエリセットでcached_beam_searchを実行し、リコール率やQPSなどの統計を出力します。ウォームアップとホットスポットのデータ統計はクエリ時間に含まれません。

詳細については、[DiskANN, A Disk-based ANNS Solution with High Recall and High QPS on Billion-scale Dataset](https://milvus.io/blog/2021-09-24-diskann.md)**を参照してください。**